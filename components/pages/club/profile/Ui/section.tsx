import React from 'react';

type Props = {};

const ProfileSection: React.SFC<Props> = ({ children }) => <section className="profile-section">{children}</section>;

export default ProfileSection;
